import type { IAnalysisSlice } from './analysisSlice.types';
import type { IFileSlice } from './fileSlice.types';
import type { ISharedSlice } from './sharedSlice.types';

// export type AnalysisState = IFileSlice & IAnalysisSlice & ISharedSlice;
export type AnalysisState = IFileSlice & IAnalysisSlice & ISharedSlice;
